# License inventory

Per CLAUDE.md §8: every external crate Attestrum depends on is tracked here with its SPDX license and the date the dependency was added. Append a row whenever a dependency lands. Sprint 1's policy (per founder-approved cross-check) is **zero new external dependencies** — `tools/diagram-linter` ships dep-free; `attestrum-core` lands with only pre-approved workspace deps.

## Pre-approved workspace dependencies

These are pre-approved by `BUILD-PLAN.md` §6.2 and `PATH-A-BRIEF.md` Part 2. Adding any of them to a crate is allowed without further approval; only ADDITIONS to this list need founder sign-off.

| Crate | Version | License (SPDX)        | Pre-approval source        |
|---|---|---|---|
| `blake3`       | `1.5`    | `Apache-2.0 OR CC0-1.0`         | BUILD-PLAN §6.2           |
| `sha2`         | `0.10`   | `MIT OR Apache-2.0`             | BUILD-PLAN §6.2           |
| `serde`        | `1`      | `MIT OR Apache-2.0`             | BUILD-PLAN §6.2           |
| `serde_json`   | `1`      | `MIT OR Apache-2.0`             | BUILD-PLAN §6.2           |
| `thiserror`    | `1`      | `MIT OR Apache-2.0`             | BUILD-PLAN §6.2           |
| `jiff`         | `0.x`    | `MIT OR Apache-2.0`             | PATH-A-BRIEF §2.1         |
| `unicode-normalization` | `0.1` | `MIT OR Apache-2.0`        | PATH-A-BRIEF §2.1         |
| `robotstxt`    | latest   | `Apache-2.0`                    | BUILD-PLAN §6.2           |
| `quick-xml`    | `0.31`   | `MIT`                           | BUILD-PLAN §6.2           |
| `clap`         | `4.5`    | `MIT OR Apache-2.0`             | BUILD-PLAN §6.2           |
| `tracing`      | latest   | `MIT`                           | BUILD-PLAN §6.2           |
| `tracing-subscriber` | latest | `MIT`                       | BUILD-PLAN §6.2           |
| `arrow`        | `52.x`   | `Apache-2.0`                    | BUILD-PLAN §6.2           |
| `parquet`      | `52.x`   | `Apache-2.0`                    | BUILD-PLAN §6.2           |
| `rocksdb`      | `0.22`   | `Apache-2.0 OR MIT OR BSD-3-Clause` | BUILD-PLAN §6.2       |
| `sigstore`     | latest   | `Apache-2.0`                    | BUILD-PLAN §6.2           |
| `tokio`        | `1.40+`  | `MIT`                           | BUILD-PLAN §6.2           |
| `reqwest`      | `0.13`   | `MIT OR Apache-2.0`             | BUILD-PLAN §6.2 (version bumped from `0.12` to match the sigstore-induced 0.13.3 lockfile entry at Sprint 5 D2 E7) |
| `iscc-lib`     | `0.4`    | `Apache-2.0`                    | PATH-A-BRIEF §2.1         |
| `image`        | `0.25`   | `MIT OR Apache-2.0`             | PATH-A-BRIEF §2.1         |
| `image_hasher` | `3.0`    | `MIT`                           | PATH-A-BRIEF §2.1         |
| `blockhash`    | `0.5`    | `Apache-2.0 OR MIT`             | PATH-A-BRIEF §2.1         |
| `hf-hub`       | `git pin` | `Apache-2.0`                   | PATH-A-BRIEF §2.3 (only approved git-pinned dep) |
| `proptest`     | `1`      | `MIT OR Apache-2.0`             | Sprint 2 E2 plan (founder-approved 2026-05-23; dev-dep only; CLAUDE.md §7.1 obligation) |
| `rayon`        | `1`      | `MIT OR Apache-2.0`             | Sprint 3 E4 (founder-approved 2026-05-24; design-implied by BUILD-PLAN §4 + §9 + `rayon-pipeline.md` but absent from §6.2 crate table) |
| `toml`         | `0.8`    | `MIT OR Apache-2.0`             | BUILD-PLAN §6.2 (row was missing from this pre-approved table prior to Sprint 3 E5 — docs-sync gap, not a fresh approval) |
| `wiremock`     | `0.6`    | `MIT OR Apache-2.0`             | Sprint 5 S5-D3 E3 plan (founder-approved 2026-05-28; dev-dep only; mocks HF Hub HTTP API for the `attestrum-publish` integration tests at `tests/publish_huggingface.rs`). |
| `tempfile`     | `3`      | `MIT OR Apache-2.0`             | Cross-cited from the "Actually-used" row added at the 2026-05-25 post-public-flip freshness-fix; promoted to this pre-approved table at S5-D3 E3 so future dev-dep additions of `tempfile` don't need to re-litigate. Both SPDX choices on the allow-list. |

## Actually-used in this workspace

Append a row when a crate is added to ANY workspace member's `Cargo.toml`. Format: `<crate>@<version> | <SPDX> | <YYYY-MM-DD> | <added in commit> | <crate(s) that depend on it>`.

| Crate | Version | License (SPDX) | Date added | Commit | Used by |
|---|---|---|---|---|---|
| `serde`      | `1.x`  | `MIT OR Apache-2.0` | 2026-05-23 | E7 | `attestrum-core` |
| `thiserror`  | `1.x`  | `MIT OR Apache-2.0` | 2026-05-23 | E7 | `attestrum-core` |
| `serde_json` | `1.x`  | `MIT OR Apache-2.0` | 2026-05-23 | E7 | `attestrum-core` (dev-dep) |
| `proptest`   | `1.x`  | `MIT OR Apache-2.0` | 2026-05-23 | Sprint 2 E2 | `attestrum-signals` (dev-dep) |
| `blake3`     | `1.5`  | `Apache-2.0 OR CC0-1.0` | 2026-05-23 | Sprint 2 E5 | `attestrum-cas` |
| `sha2`       | `0.10` | `MIT OR Apache-2.0` | 2026-05-23 | Sprint 2 E5 | `attestrum-cas` |
| `arrow`      | `55.2` | `Apache-2.0` | 2026-05-24 | Sprint 3 E3 | `attestrum-manifest` (regular + dev) — bumped from BUILD-PLAN §6.2's `52.x` because `arrow 52.2.0 + chrono 0.4.44` had a `quarter()` trait-disambiguation compile error fixed in `arrow 53+` |
| `parquet`    | `55.2` | `Apache-2.0` | 2026-05-24 | Sprint 3 E3 | `attestrum-manifest` (regular + dev) — features `arrow + zstd`, `default-features = false`. Bumped from `52.x` per same reason as `arrow`. |
| `rayon`      | `1.x`  | `MIT OR Apache-2.0` | 2026-05-24 | Sprint 3 E4 | `attestrum-pipeline` — `par_iter().fold(...).reduce(...)` per-worker accumulators for the hash + CAS-put stage per E3 cross-check. Founder-approved 2026-05-24 as a documented amendment to BUILD-PLAN §6.2 (was design-implied but missing from the crate table). |
| `clap`       | `4.x`  | `MIT OR Apache-2.0` | 2026-05-24 | Sprint 3 E5 | `attestrum-cli` — derive-macro CLI framework for `attestrum build` (and later subcommands). `features = ["derive"]`. Pre-approved per BUILD-PLAN §6.2 and the inventory's pre-approved table above. |
| `tracing`    | `0.1`  | `MIT`               | 2026-05-24 | Sprint 3 E5 | `attestrum-cli` — structured logging facade. Pre-approved per BUILD-PLAN §6.2 and the inventory's pre-approved table. |
| `tracing-subscriber` | `0.3` | `MIT`        | 2026-05-24 | Sprint 3 E5 | `attestrum-cli` — `tracing` sink with `env-filter` + `fmt` features for the RUST_LOG-style verbosity flag. Pre-approved per BUILD-PLAN §6.2. |
| `toml`       | `0.8`  | `MIT OR Apache-2.0` | 2026-05-24 | Sprint 3 E5 | `attestrum-cli` — corpus.toml deserialization per BUILD-PLAN §8.3. Pre-approved per BUILD-PLAN §6.2 (row was missing from this file's pre-approved table; added in this commit alongside the in-use row — docs-sync gap, not a fresh approval). |
| `schemars`   | `1.2`  | `MIT`               | 2026-05-24 | Sprint 4 E2.5 | `attestrum-attest` — derives JSON Schema 2020-12 from the three predicate Rust types via `#[derive(JsonSchema)]` + `schema_for!()`. Emits to `docs/schemas/{training-corpus,inclusion-proof,non-inclusion-proof}-v0.1.schema.json`. Founder-approved 2026-05-24 as a new dev/regular dep: not in BUILD-PLAN §6.2 explicit list but the standard Rust JSON-Schema derivation crate, MIT-licensed (allow-listed per CLAUDE.md §8). v1.x emits draft 2020-12 by default which matches PATH-A-BRIEF §3 schemas' `$schema` field; v0.8 emits draft 7 (wouldn't match). Added to `[workspace.dependencies]` in workspace Cargo.toml + `[dependencies]` in `crates/attestrum-attest/Cargo.toml`. |
| `sigstore`   | `0.14` | `Apache-2.0`        | 2026-05-24 | Sprint 4 E3 | `attestrum-attest` — Sigstore Bundle v0.3 sign + verify per BUILD-PLAN §3.4 / §6.2 + PATH-A-BRIEF Part 2. Pre-approved per BUILD-PLAN §6.2; pinned to v0.14 (current at 2026-05-24, has `bundle = [sign, verify]` feature flag for end-to-end Bundle v0.3 support). `default-features = false` + explicit features `bundle, fulcio, rekor, sigstore-trust-root, rustls-tls` to drop `cosign` (OCI), `mock-client`, `cached-client`, `oauth` (transitively brought in by `fulcio`), and to prefer `rustls-tls` over `native-tls` for cross-platform determinism. Requires rustc ≥ 1.88 transitively via `serde_with 3.20`; triggered the chore(toolchain) bump from 1.85 → 1.88 immediately before this commit. **Transitive-only license-allow-list expansions** added to `deny.toml` at the same commit per CLAUDE.md §8 transitive-exception pattern: `ISC` + `MIT-0` (from `aws-lc-rs` + `aws-lc-sys` via rustls), `Zlib` (from `foldhash` via newer hashbrown), `CDLA-Permissive-2.0` (from `webpki-root-certs`). All four are OSI-approved permissive; direct deps remain restricted to the CLAUDE.md §8 list. **Pre-Sprint-5 2026-05-25**: workspace `[patch.crates-io]` now overrides this dependency with the `Attestrum/sigstore-rs` fork at rev `ade5422143560cd795aae4ce59c593fe58090336` (branch `attestrum/email-optional-for-workload-identity-tokens`). The fork adds two minimal patches that make `Claims.email` optional (`src/oauth/token.rs:31`) + adjust the CSR-subject construction to handle the absent-email case (`src/bundle/sign.rs:100`). Required because sigstore-rs 0.14.0 hard-requires `email: String` and breaks every workload-identity OIDC flow with the misleading "Malformed JWT: claims JSON malformed" error — bug confirmed present in 0.12.1, 0.13.0, 0.14.0, and the upstream `main` branch. Founder-approved 2026-05-25 as a new git-pinned dep per CLAUDE.md §8; FIRST approved git-pin in the workspace (the SECOND is `hf-hub` added 2026-05-28 at S5-D3 E2; PATH-A-BRIEF Part 2.3 originally forecast hf-hub as the first pin, but the cosign-interop fix pulled sigstore-rs in earlier). License Apache-2.0 unchanged (the fork is a 16-line patch to a 2-spot Rust file; original Apache-2.0 LICENSE carries over). Pin removable once the upstream PR (tracked separately) merges and a sigstore-rs version > 0.14.0 carries the fix. |
| `zstd`       | `=0.13.3` | `MIT`           | 2026-05-24 | Sprint 4 E3.6 | `attestrum-manifest` — declared-but-not-imported direct dep that exists purely to give the cargo resolver an exact-version constraint on the compression codec underlying `parquet`'s `zstd` feature. `parquet` is what actually compresses bytes (CLAUDE.md §7 PROTECTED: ZSTD level 3); this dep entry stops a future `cargo update` from silently bumping `zstd 0.13.3` → `zstd 0.13.x` (and transitively the bundled C `zstd-sys 2.0.12+zstd.1.5.6` → newer zstd-sys → different compressed bytes), which would break the 4-target byte-identity gate on `manifest.parquet`. Founder-approved 2026-05-24 as determinism-hardening dep; MIT is already on the allow-list so no deny.toml change. The transitive deps `zstd-safe 7.2.1` (MIT/Apache-2.0) and `zstd-sys 2.0.12+zstd.1.5.6` (MIT/Apache-2.0; bundles C zstd 1.5.6) inherit allow-list coverage. |
| `attestrum-attest` | `0.0.1` | path-dep `Apache-2.0 OR MIT` (workspace) | 2026-05-24 | Sprint 4 E3.5 | `attestrum-cli` — promoted internal path-dep with explicit `version = "0.0.1"` pin (per `feedback_attestrum_path_deps_need_version.md`). Provides `TrainingCorpusPredicate` + `InTotoStatement` + `deterministic_json` + `sign()` for the `attestrum sign` subcommand. |
| `sha2`       | `0.10` | `MIT OR Apache-2.0` | 2026-05-24 | Sprint 4 E3.5 | `attestrum-cli` (promoted from `attestrum-cas`-transitive to direct) — feeds the SHA-256 half of the `DigestMap` for the in-toto Subject + the predicate's `manifest.digest_set` (BLAKE3 is Attestrum-native, SHA-256 is the in-toto / Sigstore interop requirement per BUILD-PLAN §3.4). Already in workspace deps. |
| `x509-cert`  | `0.2.5` | `Apache-2.0 OR MIT` | 2026-05-24 | Sprint 4 E4 | `attestrum-attest` — parses Sigstore Bundle's leaf cert (DER from `verificationMaterial.certificate.rawBytes` keyless OR `.x509CertificateChain.certificates[0].rawBytes` legacy chain form) to extract SAN + Fulcio OIDC-issuer extension (OID `1.3.6.1.4.1.57264.1.8` v1, fallback `1.3.6.1.4.1.57264.1.1` legacy). Promoted from sigstore-rs-transitive to direct so `attestrum_attest::identity::extract_identity` can operate on bundles without going through sigstore-rs's `pub(crate)` `CheckedBundle` internals. License already allow-listed; no `deny.toml` change. |
| `regex`      | `1.x`  | `MIT OR Apache-2.0` | 2026-05-24 | Sprint 4 E4 | `attestrum-attest` — applies the operator-supplied `--certificate-identity <REGEX>` + `--certificate-oidc-issuer <REGEX>` patterns to the extracted identity for cosign-compatible verify semantics (sigstore-rs's `bundle::verify::policy::Identity` is literal-string-only). Promoted from `tracing-subscriber`-`env-filter`-transitive to direct. License pre-approved per BUILD-PLAN §6.2's general allow-list. |
| `base64`     | `0.22` | `MIT OR Apache-2.0` | 2026-05-24 | Sprint 4 E4 | `attestrum-attest` — decodes the base64-DER of `bundle.verificationMaterial.certificate.rawBytes` for the x509-cert parser. Promoted from multiple-transitive to direct. License pre-approved per CLAUDE.md §8. |
| `unicode-normalization` | `0.1` | `MIT OR Apache-2.0` | 2026-05-25 | Sprint 5 S5-D1 E1 | `attestrum-fingerprint` — NFC normalization step of the PROTECTED text-fingerprint pipeline (`input.nfc().collect()`). Pre-approved per PATH-A-BRIEF §2.1 + the "Pre-approved workspace dependencies" table above. Added to `[workspace.dependencies]` alongside `jiff` in the same E1 commit. License unchanged from PATH-A-BRIEF's pre-approval; no `deny.toml` change. The NFC choice is itself part of the CLAUDE.md §4 PROTECTED text-fingerprint normalization — changing to NFD/NFKC/NFKD post-E1 invalidates every inclusion proof emitted to date. |
| `jiff`       | `0.2`  | `MIT OR Apache-2.0` | 2026-05-25 | Sprint 5 S5-D1 E1 | `attestrum-fingerprint` — `jiff::Timestamp::from_second` + `Timestamp::to_string` (RFC 3339 form) for the `FingerprintBundle.generated_at` field. Caller supplies `source_date_epoch: i64`; the crate never reads the system clock (preserves cross-target byte determinism per CLAUDE.md §7). Pre-approved per PATH-A-BRIEF §2.1 + the "Pre-approved workspace dependencies" table above. Added to `[workspace.dependencies]` in the same E1 commit. License pre-approved; no `deny.toml` change. v0.2 line was current at 2026-05-25 (v0.1.x's `Timestamp` shape carried forward without breaking changes; v0.2 pinned for `from_second` returning a `Result` shape used here for the InvalidTimestamp error path). |
| `image`      | `0.25` | `MIT OR Apache-2.0` | 2026-05-25 | Sprint 5 S5-D1 E2 | `attestrum-fingerprint` — `image::load_from_memory` decode in `fingerprint_image`. `default-features = false` + explicit features `["png","jpeg","webp","bmp","gif","tiff"]` to drop AVIF + HDR + PNM + ICO + TGA + DDS (formats we don't expect in published training datasets and which pull in additional decoders + their license trees). Pre-approved per PATH-A-BRIEF §2.1. **Transitive note**: even with avif feature OFF, the dep graph still reaches `rav1e v0.8.1 → libfuzzer-sys v0.4.12` (NCSA license) — added to deny.toml's transitive-only allow-list in the same commit; see deny.toml comment for trust-profile rationale. |
| `image_hasher` | `3.0` (resolves to 3.1.1) | `MIT` | 2026-05-25 | Sprint 5 S5-D1 E2 | `attestrum-fingerprint` — DCT-based 64-bit pHash via `HasherConfig::new().hash_size(8, 8).preproc_dct()`. Pre-approved per PATH-A-BRIEF §2.1. `3.0` was the published constraint in the brief; resolver picks `3.1.1` (semver-compatible). `3.0+` requires `image >=0.25, <0.26` matching our workspace. License unchanged from pre-approval. |
| `blockhash`  | `1`  | `MIT`              | 2026-05-25 | Sprint 5 S5-D1 E2 | `attestrum-fingerprint` — `blockhash::blockhash64` (the blockhash.io spec, 64-bit perceptual hash; distinct algorithm class from `image_hasher`'s `HashAlg::Blockhash` variant). **Version bump from PATH-A-BRIEF §2.1's `"0.5"` → `"1"`**: `blockhash 0.5.0` pins `image ^0.24` while the rest of the workspace + `image_hasher 3.0` require `image ^0.25` (different crate instances at the type level — `DynamicImage` from 0.24 doesn't satisfy traits expecting the 0.25 version). `blockhash 1.0.0` was published with `image ^0.25` for exactly this alignment; the `blockhash64` + `Display` + `distance` API is byte-identical to 0.5.0 (verified by reading both source trees) so no code change beyond the version pin. License remains MIT — unchanged from pre-approval. Pragmatic dep-graph fix rather than a fresh approval per CLAUDE.md §8 — the crate identity (`blockhash` from `jaehl/blockhash`) is the pre-approved scope. |
| `iscc-lib`   | `0.4.0` | `Apache-2.0` | 2026-05-25 | Sprint 5 S5-D1 E4 | `attestrum-fingerprint` — official ISO 24138:2024 ISCC Rust-core implementation (BIO-CODES project, conformance-tested polyglot). Pre-approved per PATH-A-BRIEF §2.1 + the "Pre-approved workspace dependencies" table above. Calls used: `gen_text_code_v0` + `gen_image_code_v0` + `gen_data_code_v0` + `gen_instance_code_v0` + `gen_iscc_code_v0` — all 64-bit per unit, composing `[content, data, instance]` with `wide=false`. Image content-code feeds a 32×32 grayscale `image::imageops::FilterType::Lanczos3` resize of the decoded image (1024-byte fixed array). Text content-code feeds the RAW input text (per ISCC spec; iscc-lib applies its own `text_clean`/`text_collapse`/`text_trim` internally). **PROTECTED**: version pin + text-input source + image pre-processing + bit width + composition shape are all locked at first emission per CLAUDE.md §4 (founder-approved 2026-05-25; commit footer). **Transitive license-allow-list expansion** added to `deny.toml` at the same commit per CLAUDE.md §8 transitive-exception pattern: `BSL-1.0` (Boost Software License 1.0, for `xxhash-rust v0.8.15` used internally by iscc-lib for CDC chunking). BSL-1.0 is OSI-approved + FSF-free + permissive (strictly more permissive than MIT — no attribution required for binary redistribution); same trust profile as the existing transitive-only entries (NCSA, ISC, MIT-0, Zlib, CDLA-Permissive-2.0). Direct workspace deps remain restricted to the CLAUDE.md §8 list. |
| `tempfile`   | `3` (resolves to 3.27.0) | `MIT OR Apache-2.0` | 2026-05-25 | post-public-flip freshness-fix | `tools/diagram-linter` (dev-dependency only). Used by `tests/freshness_pathspec.rs` to create ephemeral git repos for the rolling-window pathspec-exclusion integration tests. Already transitively in `Cargo.lock` via other workspace deps before this row was added; promoted to a direct dev-dep on the diagram-linter crate. Both SPDX choices already allow-listed per CLAUDE.md §8. No `deny.toml` change required. |
| `reqwest`    | `0.13` (resolves to 0.13.3) | `MIT OR Apache-2.0` | 2026-05-27 | Sprint 5 S5-D2 E7 | `attestrum-prove` — `reqwest::blocking::Client` for HuggingFace Hub + arbitrary URL Parquet manifest fetch in `dispatch_manifest_fetch` / `fetch_to_cache`. Promoted from sigstore-transitive (sigstore-rs activates reqwest's async + `rustls` features) to direct workspace dep so attestrum-prove can call the blocking client without instantiating a tokio runtime on the `prove()` call stack. Net-new feature added: `blocking` (sigstore activates async-only); existing `rustls` (= aws-lc-rs-backed `__rustls-aws-lc-rs` internal feature) reused. `default-features = false` drops `default-tls` so rustls is the only TLS implementation and cross-target build determinism stays predictable. Workspace-dependencies entry: `version = "0.13"`, features `["blocking", "rustls"]` (reqwest 0.13 renamed the pre-0.13 `rustls-tls` feature to just `rustls`). Pre-approved per BUILD-PLAN §6.2 (pre-approved table version bumped from `0.12` → `0.13` in the same commit to reflect the actual sigstore-induced lockfile pin). License already allow-listed; no `deny.toml` change required. |
| `sha2`       | `0.10` | `MIT OR Apache-2.0` | 2026-05-27 | Sprint 5 S5-D2 E7 | `attestrum-prove` (graph-promoted from `attestrum-cas`-transitive + the workspace deps table) — feeds `Sha256::digest` for the manifest-cache-key derivation (`huggingface:<repo>@<revision>` or `url:<url>` → SHA-256 → hex → cache subdir name) in `cache_key_for_source`. Pre-approved per BUILD-PLAN §6.2 + the inventory's pre-approved table. Already in `[workspace.dependencies]` since Sprint 2 E5 + listed as actually-used by `attestrum-cas` (2026-05-23) and `attestrum-cli` (2026-05-24); E7 adds the third direct-use site. |
| `wiremock`   | `0.6` (resolves to 0.6.5) | `MIT OR Apache-2.0` | 2026-05-28 | Sprint 5 S5-D3 E3 | `attestrum-publish` (dev-dep only). Spins per-test `MockServer` instances on a side `tokio::runtime::Builder::new_multi_thread()` runtime for the 5 integration tests at `tests/publish_huggingface.rs`. Tests cover: happy-path 6-op create_commit round-trip; 401→Auth; 429→Quota; 409 on create_repo with `exist_ok=true`→idempotent success; connection-refused→Network. **First wiremock introduction in the workspace** — founder-approved 2026-05-28 per CLAUDE.md §8 as a new dev-dep. License `MIT OR Apache-2.0` (allow-listed; no `deny.toml` change). **Runtime gotcha**: hf-hub's `HFClientSync` calls `runtime.block_on(...)` internally, which panics if invoked from inside an existing tokio runtime (which `#[tokio::test]` provides). The tests work around this by spinning the MockServer on a side runtime via `rt.block_on(MockServer::start())` and running the sync `publish()` call on the test thread (which has no active runtime context). Documented in the test file's module-level doc-comment so future maintainers don't accidentally migrate to `#[tokio::test]`. Pulled in dev-deps: `tokio` (`1`, features `["rt", "macros"]`) + `tempfile` (`3`) — both pre-approved per the table above. |
| `tokio`      | `1`   | `MIT`               | 2026-05-28 | Sprint 5 S5-D3 E3 | `attestrum-publish` (dev-dep only) — multi-threaded runtime driving the wiremock MockServer's listener task. First direct use of tokio in the workspace; tokio has been a transitive dep since reqwest landed at S5-D2 E7 (via sigstore-rs's async client). Features `["rt", "macros"]` — just the runtime + the proc-macros (`#[tokio::main]` etc.), no I/O drivers since wiremock pulls those in itself. Pre-approved per BUILD-PLAN §6.2 (`1.40+` line). License `MIT`. |
| `tempfile`   | `3`   | `MIT OR Apache-2.0` | 2026-05-28 | Sprint 5 S5-D3 E3 | `attestrum-publish` (dev-dep only) — second direct-use site after `tools/diagram-linter`. Builds tempdir fixture roots for the 5 integration tests at `tests/publish_huggingface.rs` (writes `manifest.parquet`, `merkle.root`, `bundle.sigstore.json` fixtures; `TempDir::Drop` cleans up at test end). License pre-approved per the table above; no `deny.toml` change. |
| `hf-hub`     | `1.0.0` (git rev `4cd6d8acd3192021381ff85210be9192b394e0eb`) | `Apache-2.0` | 2026-05-28 | Sprint 5 S5-D3 E2 | `attestrum-prove` (closes D3 refactor debt — replaces D2 E7's inline `hf_auth_header()` + `build_hf_url()` with `hf_hub::HFClientSync::dataset(owner, name).download_file(...)` for `ManifestSource::HuggingFace`; `ManifestSource::Url` continues using `reqwest::blocking`) + `attestrum-publish` (`HuggingFaceTarget::new()` constructs `HFClientSync`; `publish()` body lands E3-E6). **SECOND approved workspace git-pin per CLAUDE.md §8** (founder approval in S5-D3 E2 commit footer; first is `sigstore` added 2026-05-25). Rev `4cd6d8acd3...` from hf-hub master at 2026-05-21 (7 days old at pin time, well inside the future Sprint-6 14-day freshness gate per PATH-A-BRIEF Part 2.3 + OQ1). hf-hub 1.0.0 is the first stable release; types renamed from the brief's `Api`/`ApiBuilder`/`ApiRepo` to `HFClient`/`HFClientBuilder`/`HFClientSync` (verified directly against master pre-pin). `default-features = false` + features `["blocking", "rustls-tls"]` — `blocking` gates the sync wrapper (Attestrum's prove + publish call stacks are sync; no tokio runtime on the `prove()` stack), `rustls-tls` aliases to `reqwest/rustls` matching the rest of the workspace's TLS choice for cross-target build determinism. **Behavioral improvement**: hf-hub owns the HF Hub auth chain (env `HF_TOKEN` → `HF_TOKEN_PATH` file → `$HF_HOME/token`), richer than D2 E7's env-only logic — users with a prior `huggingface-cli login` cached token get auth for free without setting `HF_TOKEN`. The `(private dataset? set HF_TOKEN env var)` hint from E7 survives, now triggered on `HFError::AuthRequired` + `HFError::Forbidden` instead of HTTP 401/403 status codes. License Apache-2.0 (verified at the pinned rev's `hf-hub/Cargo.toml`). Pin removable once hf-hub publishes the upload API to crates.io and we can pin a regular semver constraint. **CI gate "rev ≤ 14 days old at tag time"** mandated by PATH-A-BRIEF Part 2.3 is **deferred to Sprint 6** per OQ1 resolution at D3 E1 closeout. |

## GitHub Actions consumed

Tracked for supply-chain transparency. Actions are not linked into any Rust binary, so SPDX rows in the table above don't apply — but the consumed binary is run in CI on every triggered workflow execution, so the provenance is worth recording.

| Action | Pin | Date added | Workflow | Purpose |
|---|---|---|---|---|
| `sigstore/cosign-installer` | `@v3` (the ACTION's major-version pin — distinct from the cosign binary version it installs; explicit `cosign version --json` grep-and-exit-1 on binary-version drift inside the workflow) | 2026-05-24 | `.github/workflows/cosign-interop.yml` (Sprint 4 E4.5) | Installs the latest cosign binary (v2.5.2 at 2026-05-25; cosign has not shipped v3.x yet despite the parked-plan brief's assumption that it had) so the `cosign verify-blob-attestation --new-bundle-format` interop test can run against a real cosign binary on the runner. The `--new-bundle-format` flag landed in cosign v2.3.0; the workflow's binary-version gate accepts `v2.5+` or any future `v3+`. Belt-and-suspenders pin per parked-plan tactical decision D — `@v3` is the action major-version pin; the in-workflow `case` statement catches both installer regressions and any cosign binary-version drift below v2.5 or outside the v2/v3 lines. |

## Attestrum-owned forks of external repositories

Tracked for supply-chain transparency. Forks of external repos are listed separately from direct Cargo deps because the fork itself is project-owned IP under Hyper Beam Media LLC; the Cargo dep row above records the actual binding into the workspace.

| Fork repo | Upstream | Branch | Date forked | Reason | Removal trigger |
|---|---|---|---|---|---|
| `Attestrum/sigstore-rs` | `sigstore/sigstore-rs` | `attestrum/email-optional-for-workload-identity-tokens` | 2026-05-25 | sigstore-rs 0.14.0 hard-requires `Claims.email`, breaking every workload-identity OIDC flow. See the `sigstore` row in the in-use table above for the full bug description + patch. | Upstream sigstore-rs PR merges + a sigstore-rs release > 0.14.0 carries the fix → drop the `[patch.crates-io]` entry in workspace `Cargo.toml`, archive or delete this fork. |
