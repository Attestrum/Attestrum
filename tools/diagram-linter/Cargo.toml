[package]
name = "diagram-linter"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[dev-dependencies]
# Used by tests/freshness_pathspec.rs to create ephemeral git repos for
# the rolling-window pathspec-exclusion integration tests. Already
# transitively in Cargo.lock via other workspace deps; promoted to a
# direct dev-dep here. MIT OR Apache-2.0 (both allow-listed per CLAUDE.md §8).
tempfile = "3"
